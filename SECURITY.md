# 🔒 Политика безопасности

## Сообщение о уязвимостях

Если вы обнаружили уязвимость безопасности, пожалуйста:

1. **НЕ создавайте публичный issue**
2. Отправьте email на: [security@example.com] (замените на реальный)
3. Включите детальное описание проблемы
4. Дайте нам разумное время для исправления

## Что НЕ должно попасть в репозиторий

### ⛔ Запрещено:
- API ключи (OpenRouter, Datalab, и др.)
- Пароли и credentials
- `.env` файлы с секретами
- Private SSH/GPG ключи
- JWT токены
- Database dumps с реальными данными
- Логи с чувствительной информацией
- Compiled binaries с внедренными секретами

### ✅ Разрешено:
- Исходный код
- Схема базы данных (без данных)
- Документация
- `env.example` (шаблон без секретов)
- Тесты (без реальных credentials)

## Безопасная разработка

### 1. Setup окружения

```bash
# Клонировать репозиторий
git clone https://github.com/saztonov/RD.git
cd RD

# Создать .env из шаблона
cp env.example .env

# Заполнить СВОИ ключи (НЕ коммитить!)
nano .env
```

### 2. Перед коммитом

```bash
# Проверить что не добавляются секреты
git diff --cached | grep -i "api.key\|secret\|password"

# Проверить .gitignore
git status --ignored

# Убедиться что .env не в staged
git status | grep ".env"
```

### 3. Сборка приложения

```bash
# build.py внедряет ТОЛЬКО публичные переменные
python build.py

# .env должен быть рядом с exe
dist/
  ├── CoreStructure.exe
  └── .env  ← Создать вручную, НЕ коммитить!
```

## Архитектура безопасности

### Новая архитектура (безопасная):

```
Desktop Client (.exe)
  ↓
  ТОЛЬКО: REMOTE_OCR_API_KEY
  ↓
Remote OCR Server API
  ↓
  ВСЕ секреты: Supabase, R2, OpenRouter, Datalab
```

### Преимущества:
- ✅ Секреты только на сервере
- ✅ Невозможно извлечь из exe
- ✅ Единая точка ротации ключей
- ✅ Централизованный аудит

## Ротация ключей

### Когда ротировать:
- Каждые 90 дней (плановая ротация)
- При подозрении на компрометацию
- При увольнении сотрудника с доступом
- После публичного exposure

### Как ротировать:

1. **Сгенерировать новые ключи:**
   - OpenRouter: https://openrouter.ai/keys
   - Datalab: https://www.datalab.to/settings
   - Supabase: Project Settings → API → Rotate keys
   - R2: Cloudflare → R2 → Manage API Tokens

2. **Обновить на сервере:**
   ```bash
   # Обновить .env на сервере
   nano .env

   # Перезапустить сервер
   docker compose restart
   ```

3. **Обновить клиентам:**
   - Только `REMOTE_OCR_API_KEY` (если менялся)
   - Остальные ключи остаются на сервере

4. **Revoke старые ключи:**
   - Дождаться обновления всех клиентов
   - Отозвать старые API ключи

## Инциденты безопасности

### Если секреты попали в git:

1. **Немедленно ротировать все ключи**
2. **Очистить историю git:**
   ```bash
   # BFG Repo-Cleaner (рекомендуется)
   bfg --replace-text passwords.txt

   # Или git filter-branch
   git filter-branch --force --index-filter \
     'git rm --cached --ignore-unmatch .env' \
     --prune-empty --tag-name-filter cat -- --all

   # Force push (ОПАСНО!)
   git push --force --all
   ```

3. **Уведомить команду**
4. **Провести аудит безопасности**

### Если exe с секретами был распространен:

1. **Ротировать ВСЕ ключи**
2. **Пересобрать с безопасным build.py**
3. **Уведомить пользователей об обновлении**
4. **Установить deadline для старых версий**

## Мониторинг

### GitHub Security Features (включить):

- [x] Secret scanning (автоматически)
- [x] Dependabot alerts
- [ ] Code scanning (CodeQL)
- [ ] Branch protection rules

### CI/CD проверки:

- [x] TruffleHog (секреты)
- [x] GitLeaks (секреты)
- [x] pip-audit (уязвимости)
- [ ] Bandit (статический анализ)
- [ ] SAST сканирование

## Best Practices

### ✅ DO:
- Использовать переменные окружения для секретов
- Хранить `.env` локально (не в git)
- Регулярно обновлять зависимости
- Использовать HTTPS для API запросов
- Логировать security events
- Проводить code review

### ⛔ DON'T:
- Хардкодить секреты в коде
- Коммитить `.env` файлы
- Внедрять секреты в compiled binaries
- Использовать одинаковые ключи для dev/prod
- Шарить секреты в Slack/Email
- Игнорировать security alerts

## Инструменты

### Pre-commit hooks:
```bash
# Установить pre-commit
pip install pre-commit

# .pre-commit-config.yaml
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: latest
    hooks:
      - id: gitleaks
```

### Локальное сканирование:
```bash
# TruffleHog
trufflehog git file://. --only-verified

# GitLeaks
gitleaks detect --source . --verbose

# Detect-secrets
detect-secrets scan
```

## Контакты

- **Security Team:** security@example.com (замените)
- **GitHub Security Advisories:** https://github.com/saztonov/RD/security/advisories
- **Documentation:** [SECURITY_AUDIT.md](SECURITY_AUDIT.md)

## Версионирование

- **Дата создания:** 2026-01-03
- **Последнее обновление:** 2026-01-03
- **Версия:** 1.0
- **Следующий review:** 2026-04-03
