# Pre-commit Hooks - Руководство по настройке

## Что это такое?

Pre-commit hooks - это автоматические проверки, которые запускаются **перед каждым коммитом** и предотвращают:
- ❌ Коммит секретов (API ключи, пароли)
- ❌ Коммит .env файлов
- ❌ Коммит больших файлов (>500KB)
- ❌ Коммит с синтаксическими ошибками
- ❌ Коммит приватных SSH/GPG ключей

## 🚀 Быстрая установка

### Windows (PowerShell):
```powershell
# Запустить скрипт установки
.\setup-git-hooks.ps1
```

### Linux/Mac (Bash):
```bash
# Сделать исполняемым и запустить
chmod +x setup-git-hooks.sh
./setup-git-hooks.sh
```

### Ручная установка:
```bash
# 1. Установить pre-commit
pip install pre-commit

# 2. Установить hooks
pre-commit install

# 3. Первая проверка (опционально)
pre-commit run --all-files
```

## 📋 Что проверяется?

### 1. Секреты и ключи (GitLeaks)
- ✓ API ключи (OpenRouter, Datalab, и др.)
- ✓ AWS credentials
- ✓ JWT токены
- ✓ Private keys (SSH, GPG)
- ✓ Пароли и credentials

### 2. Файлы (detect-secrets)
- ✓ .env файлы
- ✓ Хардкоженные секреты в коде
- ✓ Подозрительные паттерны

### 3. Качество кода
- ✓ Python синтаксис (AST)
- ✓ Форматирование (Black, isort)
- ✓ Линтинг (Flake8)
- ✓ YAML/JSON валидность

### 4. Общие проверки
- ✓ Большие файлы (>500KB)
- ✓ Merge конфликты
- ✓ Trailing whitespace
- ✓ End of file newline

### 5. Кастомные проверки
- ✓ Блокировка коммитов в main/master
- ✓ Проверка на .env в staged
- ✓ Поиск R2 Account ID
- ✓ Hardcoded secrets в diff

## 🔧 Использование

### Автоматически (рекомендуется):
```bash
# Просто коммитьте как обычно
git add .
git commit -m "feat: добавлена новая функция"

# Hooks запустятся автоматически!
```

### Вручную:
```bash
# Проверить все файлы
pre-commit run --all-files

# Проверить конкретный hook
pre-commit run gitleaks --all-files
pre-commit run detect-secrets

# Проверить только staged файлы
pre-commit run
```

### Обновление hooks:
```bash
# Обновить версии hooks
pre-commit autoupdate

# Переустановить hooks
pre-commit install --install-hooks
```

## ⚠️ Что делать если hook блокирует коммит?

### 1. Найден секрет в коде:
```
[ERROR] gitleaks detected secrets in your commit
File: config.py:10
Secret: OPENROUTER_API_KEY = "sk-or-..."
```

**Решение:**
1. Удалить секрет из кода
2. Использовать `os.getenv("OPENROUTER_API_KEY")`
3. Добавить ключ в `.env` (который в `.gitignore`)

### 2. Попытка закоммитить .env:
```
[ERROR] Attempting to commit .env file!
```

**Решение:**
1. `git reset HEAD .env` (unstage файл)
2. Проверить что `.env` в `.gitignore`
3. Использовать `env.example` для шаблона

### 3. Большой файл:
```
[ERROR] File is 1.5 MB, max allowed is 500 KB
```

**Решение:**
1. Использовать Git LFS для больших файлов
2. Или добавить в `.gitignore`
3. Или загрузить в S3/R2

### 4. Синтаксическая ошибка Python:
```
[ERROR] Check python AST - Failed
File: app/main.py has syntax error
```

**Решение:**
1. Исправить синтаксис
2. Запустить `python -m py_compile app/main.py`

## 🛠️ Конфигурация

### Отключить конкретный hook:
Отредактируйте `.pre-commit-config.yaml`:
```yaml
repos:
  - repo: https://github.com/psf/black
    rev: 23.12.1
    hooks:
      - id: black
        # Временно отключить
        # stages: [manual]
```

### Исключить файлы:
```yaml
# В корне .pre-commit-config.yaml
exclude: |
  (?x)^(
      build/|
      dist/|
      __pycache__/
  )
```

### Добавить свой hook:
```yaml
- repo: local
  hooks:
    - id: my-custom-check
      name: My Custom Check
      entry: bash -c 'echo "Running custom check"'
      language: system
```

## 🚨 Обход hooks (НЕ РЕКОМЕНДУЕТСЯ!)

```bash
# Пропустить ВСЕ hooks (ОПАСНО!)
git commit --no-verify -m "Skip hooks"

# Или
git commit -n -m "Skip hooks"
```

**ВНИМАНИЕ:** Используйте только в крайних случаях! Вы рискуете закоммитить секреты.

## 📊 Статистика и логи

```bash
# Посмотреть установленные hooks
ls -la .git/hooks/

# История запусков
cat .git/hooks/pre-commit

# Детальный вывод
pre-commit run --all-files --verbose

# С цветами
pre-commit run --all-files --color=always
```

## 🔍 Отладка проблем

### Hook не запускается:
```bash
# Переустановить
pre-commit uninstall
pre-commit install

# Проверить установку
pre-commit --version
ls -la .git/hooks/pre-commit
```

### Hook падает с ошибкой:
```bash
# Запустить с debug
pre-commit run --all-files --verbose

# Проверить синтаксис конфига
pre-commit validate-config

# Очистить кеш
pre-commit clean
```

### Обновление не работает:
```bash
# Принудительное обновление
pre-commit autoupdate
pre-commit install --install-hooks --force
```

## 📚 Дополнительные ресурсы

- [Pre-commit документация](https://pre-commit.com/)
- [GitLeaks](https://github.com/gitleaks/gitleaks)
- [Detect-secrets](https://github.com/Yelp/detect-secrets)
- [Список доступных hooks](https://pre-commit.com/hooks.html)

## ✅ Checklist после установки

- [ ] Pre-commit установлен (`pre-commit --version`)
- [ ] Hooks зарегистрированы (`.git/hooks/pre-commit` существует)
- [ ] Первая проверка пройдена (`pre-commit run --all-files`)
- [ ] `.secrets.baseline` создан (для detect-secrets)
- [ ] Git фильтры настроены (`git config --get filter.git-secrets.clean`)
- [ ] .env файлы не в git (`git ls-files | grep .env` → пусто)
- [ ] Тестовый коммит прошел успешно

## 🎯 Best Practices

1. **Регулярно обновляйте hooks:**
   ```bash
   pre-commit autoupdate
   ```

2. **Проверяйте перед push:**
   ```bash
   pre-commit run --all-files
   ```

3. **Не обходите hooks без крайней необходимости**

4. **Добавьте в CI/CD:**
   ```yaml
   # .github/workflows/ci.yml
   - name: Run pre-commit
     run: pre-commit run --all-files
   ```

5. **Обучите команду:**
   - Покажите как работают hooks
   - Объясните как исправлять ошибки
   - Поделитесь этим документом

---

**Помните:** Pre-commit hooks - это первая линия защиты от утечки секретов! 🛡️
