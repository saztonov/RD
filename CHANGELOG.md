# Changelog

## [1.2.0] - 2025-11-29

### ✨ Добавлено

#### Обновлённая модель данных (`app/models.py`)

**Новые Enums:**
- `BlockSource` — источник создания блока (USER/AUTO)

**Переработанный класс Block:**
- `id: str` — уникальный идентификатор (UUID)
- `page_index: int` — индекс страницы
- `coords_px: Tuple[int, int, int, int]` — координаты в пикселях (x1, y1, x2, y2)
- `coords_norm: Tuple[float, float, float, float]` — нормализованные координаты (0..1)
- `category: str` — описание/группа блока
- `block_type: BlockType` — тип блока
- `source: BlockSource` — источник (USER/AUTO)
- `image_file: Optional[str]` — путь к кропу
- `ocr_text: Optional[str]` — результат OCR

**Новый класс PageModel:**
- Интеграция с PIL.Image
- Методы для работы с блоками (add, remove, get_by_id, filter)
- Properties: width, height, size
- Сериализация с/без изображения

**Статические методы Block:**
- `generate_id()` — генерация UUID
- `px_to_norm()` — конвертация координат в нормализованные
- `norm_to_px()` — конвертация обратно в пиксели
- `create()` — фабричный метод с автовычислением norm координат

**Методы экземпляра Block:**
- `get_width_height_px()` — размеры в пикселях
- `get_width_height_norm()` — размеры нормализованные
- `update_coords_px()` — обновление координат с пересчётом

**Helper функции:**
- `create_block_from_legacy()` — конвертация из старого формата (x, y, w, h)
- `block_to_legacy_coords()` — конвертация в старый формат
- `coords_xywh_to_xyxy()` — (x, y, w, h) → (x1, y1, x2, y2)
- `coords_xyxy_to_xywh()` — (x1, y1, x2, y2) → (x, y, w, h)

**Обратная совместимость:**
- Старые классы `Page` и `Document` сохранены для GUI
- Помечены как legacy в документации

#### Документация

- **`docs/DATA_MODELS.md`** — полная документация по моделям данных
  - Описание всех классов и методов
  - Примеры использования координатных систем
  - Паттерны работы с блоками
  - Сериализация/десериализация
  
- **`examples/test_models.py`** — 7 примеров работы с моделями
  - Создание блоков
  - Конвертация координат (px ↔ norm)
  - Legacy конвертация
  - Работа с PageModel
  - Сериализация в JSON
  - Обновление координат при изменении zoom
  - Helper функции

### 🔧 Улучшено

#### Системы координат

**Двойная система координат:**
- **coords_px** — абсолютные координаты в пикселях на отрендеренном изображении
- **coords_norm** — относительные координаты (0..1), не зависят от zoom

**Преимущества:**
- Перенос разметки между PDF с разными размерами
- Адаптация к разным уровням zoom
- Точное восстановление координат при масштабировании

**Автоматическая синхронизация:**
- При создании блока через `Block.create()` norm координаты вычисляются автоматически
- При обновлении px через `update_coords_px()` norm пересчитываются

#### Идентификация блоков

- Уникальные UUID для каждого блока
- Быстрый поиск по ID через `PageModel.get_block_by_id()`
- Отслеживание изменений блоков между версиями

#### Категоризация

- Поле `category` для группировки блоков ("Заголовок", "Параметры", "Схема")
- Поле `source` для различения ручных и автоматических блоков
- Фильтрация по типу и источнику через методы PageModel

### 📦 Технические детали

**Формат координат:**
- Новый формат: (x1, y1, x2, y2) — координаты углов прямоугольника
- Старый формат: (x, y, width, height) — поддерживается через helper функции

**Нормализация:**
```python
x_norm = x_px / page_width
y_norm = y_px / page_height
```

**Обратная конвертация:**
```python
x_px = int(x_norm * page_width)
y_px = int(y_norm * page_height)
```

**Сериализация:**
- JSON-friendly формат
- Опциональное включение изображения (base64)
- Все типы конвертируются в сериализуемые (tuple → list)

### 💡 Практические применения

**1. Перенос разметки при изменении zoom:**
```python
# Исходная разметка zoom=2.0 (1600x2400)
block = Block.create(..., coords_px=(100, 200, 500, 600))

# Новая страница zoom=3.0 (2400x3600)
new_coords = Block.norm_to_px(block.coords_norm, 2400, 3600)
# Пропорции сохранены!
```

**2. Перенос разметки между версиями PDF:**
```python
# Сохраняем norm координаты в JSON
# При открытии новой версии PDF пересчитываем в px
```

**3. Фильтрация блоков:**
```python
user_blocks = page.get_blocks_by_source(BlockSource.USER)
text_blocks = page.get_blocks_by_type(BlockType.TEXT)
```

### 📝 Примечания

- Legacy классы `Page` и `Document` сохранены для совместимости с GUI
- Рекомендуется использовать `PageModel` и новый `Block` для новых функций
- Миграция старого кода: см. helper функции `create_block_from_legacy()`

---

## [1.1.0] - 2025-11-29

### ✨ Добавлено

#### PDF Рендеринг (`app/pdf_utils.py`)

**Новые функции:**
- `open_pdf(path: str) -> fitz.Document` 
  - Открытие PDF с полной валидацией
  - Проверка существования файла и расширения
  - Детальная обработка ошибок (FileNotFoundError, ValueError)
  
- `render_page_to_image(doc, page_index, zoom=2.0) -> PIL.Image`
  - Рендеринг одной страницы с сохранением пропорций
  - Валидация параметров (zoom > 0, корректный индекс)
  - Оптимизированная конвертация fitz → PIL
  
- `render_all_pages(doc, zoom=2.0) -> List[PIL.Image]`
  - Пакетный рендеринг всех страниц
  - Прогресс-логирование каждые 10 страниц
  - Устойчивость к ошибкам на отдельных страницах

**Улучшения класса PDFDocument:**
- Интеграция с новыми функциями
- Добавлен метод `render_all()`
- Улучшенная обработка ошибок

#### Логирование

**Модуль `app/main.py`:**
- Функция `setup_logging(log_level)` для конфигурации
- Двойной вывод: файл (`logs/app.log`) + консоль
- Форматирование с временными метками
- Обработка критических ошибок при запуске

**Логирование в модулях:**
- `app/pdf_utils.py` — все операции с PDF (открытие, рендеринг)
- `app/annotation_io.py` — сохранение/загрузка разметки
- `app/ocr.py` — инициализация и работа OCR
- `app/cropping.py` — экспорт кропов с прогрессом

**Уровни логирования:**
- `INFO` — ключевые события (открытие файлов, завершение операций)
- `DEBUG` — детальная информация (размеры страниц, количество символов)
- `WARNING` — предупреждения (отсутствие .pdf расширения)
- `ERROR` — ошибки (файл не найден, повреждённый PDF)

#### Документация

- **`docs/pdf_rendering.md`** — полная документация по рендерингу PDF
  - Описание всех функций с примерами
  - Таблица параметров zoom (DPI, размеры файлов)
  - Паттерны обработки ошибок
  - Рекомендации по производительности
  
- **`docs/QUICK_START.md`** — быстрый старт для разработчиков
  - Базовые примеры кода
  - Настройка логирования
  - Параметры zoom

- **`examples/test_pdf_rendering.py`** — тестовые примеры
  - Рендеринг одной страницы
  - Рендеринг всех страниц
  - Сравнение разных zoom
  - Обработка ошибок

### 🔧 Улучшено

#### Обработка ошибок
- Специфичные исключения для разных типов ошибок
- Подробные сообщения об ошибках
- Логирование с трассировкой стека (exc_info=True)

#### Производительность
- Прогресс-логирование для длительных операций
- Рекомендации по кешированию страниц
- Оптимальные значения zoom по умолчанию

#### Качество кода
- Полная типизация функций
- Подробная документация в docstrings
- Валидация входных параметров
- Соответствие PEP 8

### 📦 Технические детали

**Зависимости:**
- PyMuPDF (fitz) — рендеринг PDF
- Pillow (PIL) — работа с изображениями
- logging (stdlib) — логирование

**Масштабирование (zoom):**
- Одинаковое по X и Y для сохранения пропорций
- Диапазон: 1.0 (72 DPI) до 4.0 (288 DPI)
- Рекомендуется: 2.0 (144 DPI) для баланса качества/производительности

**Формат изображений:**
- Всегда RGB (без альфа-канала)
- PNG для примеров (можно использовать любой формат PIL)
- Размеры зависят от zoom и размера страницы PDF

### 📝 Примечания

- Все функции полностью обратно совместимы с существующим кодом
- Класс `PDFDocument` сохранён для совместимости с GUI
- Логирование не влияет на производительность в режиме INFO

---

## [1.0.0] - 2025-11-29

### Начальная версия

- Базовая структура проекта
- GUI на PySide6
- Ручная разметка блоков
- Автоматическая сегментация (OpenCV)
- OCR через pytesseract
- Экспорт в Markdown
- Перенос разметки на новый PDF

