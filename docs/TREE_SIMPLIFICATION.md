# Упрощение дерева проектов

## Выполненные изменения

### 1. Удалены типы файлов результатов
**Файл:** `app/tree_models.py`
- Удалены `RESULT_MD` и `RESULT_ZIP` из enum `FileType`
- Остались только: PDF, ANNOTATION, CROP, IMAGE, OCR_HTML, RESULT_JSON

### 2. Убрана загрузка файлов в дереве
**Файл:** `app/gui/project_tree_widget.py`
- Удален метод `_load_node_files()` (91 строка кода)
- Убран placeholder "📎 Файлы..." для документов
- Убрана обработка `files_placeholder` в `_on_item_expanded()`
- **Результат:** В дереве отображаются только узлы (папки) и PDF-документы

### 3. Очистка операций с результатами
**Файл:** `app/gui/remote_ocr_job_operations.py`
- Удалено удаление MD файлов из R2 и кэша
- Обновлен метод `clear_ocr_results()` - убраны операции с RESULT_MD и RESULT_ZIP
- Остались только операции с CROP файлами

**Файл:** `app/gui/remote_ocr_panel.py`
- Обновлены комментарии - убраны упоминания markdown

### 4. Операции со скачиванием/удалением
**Файл:** `app/gui/tree_folder_ops.py`
- Убрано скачивание .md файлов при скачивании папки

**Файл:** `app/gui/tree_cache_ops.py`
- Убрано удаление .md файлов из R2 при удалении документов
- Убрано удаление .md файлов из локального кэша

**Файл:** `app/gui/file_download.py`
- Обновлены комментарии

**Файл:** `app/tree_client.py`
- Обновлены комментарии

## Итоговый результат

✅ В дереве проектов отображаются только:
   - Узлы иерархии (Проект → Стадия → Раздел → Папка заданий)
   - PDF-документы (как конечные узлы)

✅ Полностью удалены:
   - Типы файлов RESULT_MD и RESULT_ZIP
   - Загрузка дочерних файлов документов в дерево
   - Операции скачивания/удаления markdown результатов
   - Операции с ZIP архивами результатов

✅ Синтаксис всех измененных файлов проверен
